[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA redirect — wszystkie trasy obsługuje index.html (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
